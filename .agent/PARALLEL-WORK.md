# 병렬 작업 분배 전략

> **버전**: v3.0
> **작성일**: 2026-02-04
> **기반 문서**: [PLAN.md](./PLAN.md)

---

## 0. 핵심 변경 사항 (v2.0 → v3.0)

> **변경**: "4인 실무 체제" → "5인 오케스트레이션 체제 (지휘+실무) + 새 세션 리뷰"로 확장
> **목적**: 효율적인 관리와 코드 품질 보증을 위해 지휘자(Architect) 추가 및 병렬 리뷰 시스템 도입

| 항목 | v2.0 | v3.0 |
|------|------|------|
| Agent 수 | 4명 (실무) | 5명 (지휘+실무) |
| 구조 | 단순 병렬 작업 | 오케스트레이션 루프 |
| 워크플로우 | 작업 → 동기화 | 지시 → 구현 → 리뷰 → 병합 |
| 품질 보증 | 셀프 리뷰 | 새 세션 병렬 리뷰 (같은 워크트리) |
| 문서 관리 | 분산 | 중앙집중 (Agent 0) |

---

## 1. 디렉토리 구조 (5인 체제 + 새 세션 리뷰)

```
S14P11C104/                          # Team Repo Root
├── web/                             # Team GitLab Sync Target
├── sync.sh                          # Sync Script (sh → web)
│
└── sh/                              # Personal GitHub Container
    ├── main/                        # Original Repo Base (DO NOT WORK HERE)
    │   ├── .git/                    # Git 저장소 (공유)
    │   ├── frontend/
    │   ├── backend/
    │   └── .agent/
    │
    ├── agent-0/                     # Worktree: Agent 0 (COMMANDER - 지휘 및 문서 관리)
    │   ├── frontend/
    │   ├── backend/
    │   └── .agent/                  # 🔒 문서 관리 권한
    │
    ├── agent-1/                     # Worktree: Agent 1 (BE-INFRA/AUTH)
    │   ├── frontend/
    │   ├── backend/
    │   └── .agent/
    │
    ├── agent-2/                     # Worktree: Agent 2 (FE-INFRA/ELDER)
    │   ├── frontend/
    │   ├── backend/
    │   └── .agent/
    │
    ├── agent-3/                     # Worktree: Agent 3 (DB-SCHEMA/ROBOT)
    │   ├── frontend/
    │   ├── backend/
    │   └── .agent/
    │
    └── agent-4/                     # Worktree: Agent 4 (CONTRACTS/WEBSOCKET)
        ├── frontend/
        ├── backend/
        └── .agent/
```

### 핵심 규칙

| 규칙 | 설명 |
|------|------|
| **main 폴더는 작업 금지** | `sh/main/`은 워크트리 생성 전용. 실제 작업은 `sh/agent-N/`에서만 수행 |
| **각 Agent는 전용 폴더 사용** | Agent N은 `sh/agent-N/`에서만 작업 |
| **동일 브랜치 체크아웃 금지** | 같은 브랜치를 여러 워크트리에서 동시 체크아웃 불가 (Git 제약) |
| **Agent 0은 문서 전담** | `.agent/` 폴더 내 모든 문서의 소유권 보유 |
| **코드 리뷰는 새 세션** | 작업 완료 후 같은 워크트리에서 새 Claude 세션을 띄워 리뷰 수행 |
| **⚠️ Push 시 브랜치 명시** | 모든 워크트리가 `.git`을 공유하므로 `git push origin <브랜치명>` 필수 |

> [!CAUTION]
> **Git Worktree Push 주의사항**
> 
> 모든 워크트리(agent-0~4)는 같은 `.git` 저장소를 공유합니다.
> 따라서 `git push`만 실행하면 **모든 브랜치가 Push**될 수 있습니다!
> 
> ```bash
> # ✅ 올바른 방법: 자기 브랜치만 Push
> git push origin feature/phase0-be-infra
> 
> # ❌ 위험한 방법: 모든 브랜치 Push
> git push origin --all
> git push  # 설정에 따라 여러 브랜치 Push 가능
> ```
> 
> **권장 설정**:
> ```bash
> git config push.default current  # 현재 브랜치만 Push
> ```

> **실행 체크리스트 (SSH + 네트워크 오류 대응)**:
> ```bash
> # 0) 자신 worktree 확인
> pwd
>
> # 1) SSH remote 확인
> git remote -v
> # origin: git@github.com:sonic8-8/silver-care-bot.git
>
> # 2) SSH 인증 확인
> ssh -o StrictHostKeyChecking=accept-new -T git@github.com
>
> # 3) Push 기본 정책 확인
> git config push.default current
>
> # 4) 브랜치 명시 push
> git push origin <현재브랜치명>
> ```
>
> `Could not resolve host/hostname github.com` 발생 시:
> - 동일 push 명령을 네트워크 권한 상승(`require_escalated`)으로 재실행
> - `git push` 단독 실행은 금지 (반드시 `origin <브랜치명>` 명시)

---

## 2. 환경 설정 가이드 (Phase 0 사전 작업)

> 에이전트 실행 전, 개발자가 수행해야 할 워크트리 생성 작업

### 2.1 사전 조건

```bash
# 현재 위치 확인
cd /mnt/c/Users/SSAFY/Desktop/S14P11C104/sh/main

# 브랜치 최신화
git fetch origin
git checkout develop
git pull origin develop
```

### 2.2 Worktree 생성 (Agent 0~4)

```bash
# ============================================
# Agent 0: 지휘자 Worktree 생성
# ============================================
git worktree add ../agent-0 -b management/architect

# ============================================
# Agent 1~4: 실무 역할 Worktree 생성 (Phase 0)
# ============================================

# Agent 1: Backend Infrastructure
git worktree add ../agent-1 -b feature/phase0-be-infra

# Agent 2: Frontend Infrastructure
git worktree add ../agent-2 -b feature/phase0-fe-infra

# Agent 3: Database Schema
git worktree add ../agent-3 -b feature/phase0-db-schema

# Agent 4: Contracts & Mocks
git worktree add ../agent-4 -b feature/phase0-contracts
```

### 2.3 Phase 1용 Worktree 전환

```bash
# ============================================
# Phase 0 완료 후 → Phase 1 브랜치로 전환
# ============================================

# 먼저 develop에 Phase 0 머지 완료 후 실행

# Agent 1: Auth 도메인
cd ../agent-1
git checkout -b feature/phase1-auth origin/develop

# Agent 2: Elder 도메인
cd ../agent-2
git checkout -b feature/phase1-elder origin/develop

# Agent 3: Robot 도메인
cd ../agent-3
git checkout -b feature/phase1-robot origin/develop

# Agent 4: WebSocket
cd ../agent-4
git checkout -b feature/phase1-websocket origin/develop
```

### 2.4 Worktree 상태 확인

```bash
# 현재 워크트리 목록 확인
cd /mnt/c/Users/SSAFY/Desktop/S14P11C104/sh/main
git worktree list

# 예상 출력 (5인 체제):
# /mnt/c/.../sh/main     abc1234 [develop]
# /mnt/c/.../sh/agent-0  xyz0000 [management/architect]
# /mnt/c/.../sh/agent-1  def5678 [feature/phase0-be-infra]
# /mnt/c/.../sh/agent-2  ghi9012 [feature/phase0-fe-infra]
# /mnt/c/.../sh/agent-3  jkl3456 [feature/phase0-db-schema]
# /mnt/c/.../sh/agent-4  mno7890 [feature/phase0-contracts]
```

### 2.5 Worktree 정리 (Phase 완료 후)

```bash
# 워크트리 제거 (브랜치 머지 완료 후)
git worktree remove ../agent-0
git worktree remove ../agent-1
git worktree remove ../agent-2
git worktree remove ../agent-3
git worktree remove ../agent-4

# 또는 강제 제거 (변경사항 있을 경우)
git worktree remove --force ../agent-N
```

---

## 3. Agent 역할 정의 (5인 체제)

### Agent 역할 요약 테이블

| Agent | 역할 코드 | 역할명 | 물리적 폴더 | 주요 책임 |
|-------|----------|--------|------------|----------|
| **0** | COMMANDER | 지휘자 | `sh/agent-0/` | PRD, PLAN 분석 후 작업 지시(Prompt) 작성, 일정 관리, 병합 승인 |
| **1** | BE-INFRA/AUTH | 백엔드 인프라/인증 | `sh/agent-1/` | Spring Boot 설정, JWT 인증, 보안 구성 |
| **2** | FE-INFRA/ELDER | 프론트엔드 인프라/노인관리 | `sh/agent-2/` | Vite+React 설정, 노인 CRUD UI |
| **3** | DB-SCHEMA/ROBOT | DB 스키마/로봇 | `sh/agent-3/` | Flyway, Entity, 로봇 제어 API |
| **4** | CONTRACTS/WEBSOCKET | 계약/웹소켓 | `sh/agent-4/` | API 규격, MSW, STOMP 설정 |

### Agent 0 (COMMANDER) - 지휘자

```
┌─────────────────────────────────────────────────────────────┐
│                     Agent 0: COMMANDER                      │
├─────────────────────────────────────────────────────────────┤
│ 📁 물리적 위치: sh/agent-0/                                  │
│ 🌿 브랜치: management/architect                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📋 주요 책임:                                               │
│  • PRD.md, PLAN.md 분석 및 작업 분배                         │
│  • Agent 1~4에게 작업 지시서(Prompt) 작성                    │
│  • 일정 관리 및 마일스톤 추적                                 │
│  • 리뷰 결과 확인 후 병합 승인                               │
│  • 최종 develop 병합 실행                                    │
│                                                             │
│  📄 소유 문서:                                               │
│  • .agent/PLAN.md                                           │
│  • .agent/PARALLEL-WORK.md                                  │
│  • .agent/ADR.md                                            │
│  • .agent/PRD.md                                            │
│  • .agent/HANDOFF.md                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 실무 Agent (1~4) Phase별 역할

| Agent | Phase 0 역할 | Phase 1 역할 | Phase 0 브랜치 | Phase 1 브랜치 |
|-------|-------------|-------------|---------------|---------------|
| **1** | BE-INFRA | AUTH | `feature/phase0-be-infra` | `feature/phase1-auth` |
| **2** | FE-INFRA | ELDER | `feature/phase0-fe-infra` | `feature/phase1-elder` |
| **3** | DB-SCHEMA | ROBOT | `feature/phase0-db-schema` | `feature/phase1-robot` |
| **4** | CONTRACTS | WEBSOCKET | `feature/phase0-contracts` | `feature/phase1-websocket` |

---

## 4. 오케스트레이션 워크플로우

### 4.1 오케스트레이션 루프 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                   5인 체제 + 새 세션 리뷰 오케스트레이션                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌──────────────────┐                                                      │
│   │   Agent 0        │                                                      │
│   │   COMMANDER      │                                                      │
│   │   (지휘자)       │                                                      │
│   └────────┬─────────┘                                                      │
│            │                                                                │
│            │ ①  Instruction (작업 지시서 작성 및 배포)                        │
│            ▼                                                                │
│   ┌────────────────────────────────────────────────────┐                    │
│   │              실무 Agent Pool (병렬 작업)             │                    │
│   │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐  │                    │
│   │  │ Agent 1 │ │ Agent 2 │ │ Agent 3 │ │ Agent 4 │  │                    │
│   │  │ BE/AUTH │ │ FE/ELDER│ │ DB/ROBOT│ │ WS/MOCK │  │                    │
│   │  └─────────┘ └─────────┘ └─────────┘ └─────────┘  │                    │
│   └────────────────────────┬───────────────────────────┘                    │
│                            │                                                │
│                            │ ②  Implementation (코딩 및 테스트)              │
│                            ▼                                                │
│   ┌────────────────────────────────────────────────────┐                    │
│   │     ③ 병렬 리뷰 (각 워크트리에서 새 Claude 세션)      │                    │
│   │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐  │                    │
│   │  │Reviewer │ │Reviewer │ │Reviewer │ │Reviewer │  │                    │
│   │  │   A     │ │   B     │ │   C     │ │   D     │  │                    │
│   │  │(새Claude)│ │(새Claude)│ │(새Claude)│ │(새Claude)│  │                    │
│   │  └─────────┘ └─────────┘ └─────────┘ └─────────┘  │                    │
│   └────────────────────────┬───────────────────────────┘                    │
│                            │                                                │
│                            │ Approve ✅ / Reject ❌                            │
│                            ▼                                                │
│   ┌──────────────────┐                                                      │
│   │   Agent 0        │◄─── ④  Merge (리뷰 통과 시 develop 병합)              │
│   │   COMMANDER      │                                                      │
│   └────────┬─────────┘                                                      │
│            │                                                                │
│            │ ⑤  Sync (sync.sh 실행)                                         │
│            ▼                                                                │
│   ┌──────────────────┐                                                      │
│   │   Team Repo      │                                                      │
│   │   (web/)         │                                                      │
│   └──────────────────┘                                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 워크플로우 단계별 상세

#### ① Instruction (Agent 0)

```
┌─────────────────────────────────────────────────────────────┐
│  단계 1: 작업 지시 (Agent 0 → Agent 1~4)                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📋 입력:                                                    │
│  • PRD.md 요구사항                                           │
│  • PLAN.md 기술 설계                                         │
│  • 현재 Sprint 목표                                          │
│                                                             │
│  📤 출력:                                                    │
│  • 각 Agent별 작업 지시서 (Prompt)                           │
│  • 예상 소요 시간                                            │
│  • 완료 기준 (Definition of Done)                           │
│  • 의존성 및 우선순위                                        │
│                                                             │
│  📍 실행 위치: sh/agent-0/                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### ② Implementation (Agent 1~4)

```
┌─────────────────────────────────────────────────────────────┐
│  단계 2: 구현 (Agent 1~4, 병렬 수행)                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📋 입력:                                                    │
│  • Agent 0의 작업 지시서                                     │
│  • 기존 코드베이스                                           │
│                                                             │
│  🔨 수행 작업:                                               │
│  • 피처 구현                                                 │
│  • 단위 테스트 작성                                          │
│  • 로컬 통합 테스트                                          │
│                                                             │
│  📤 출력:                                                    │
│  • 커밋된 코드 (feature/* 브랜치)                           │
│  • PR 생성                                                   │
│                                                             │
│  📍 실행 위치: sh/agent-N/ (각자 폴더)                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### ③ Review (새 Claude 세션 - 병렬 리뷰)

```
┌─────────────────────────────────────────────────────────────┐
│  단계 3: 병렬 리뷰 (새 Claude 세션 4개)                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  � 실행 방식:                                                 │
│  • 각 워크트리(agent-N)에서 새 Claude 세션 실행             │
│  • 4개 워크트리에서 병렬로 리뷰 진행                            │
│  • 브랜치 이동 없이 제자리 리뷰                               │
│                                                             │
│  📋 입력:                                                    │
│  • Agent 1~4가 작성한 리뷰 요청 프롬프트                        │
│  • 각 Agent의 커밋된 코드                                    │
│                                                             │
│  🔍 검수 항목:                                               │
│  • CLAUDE.md 컨벤션 준수                                     │
│  • 코드 스타일/품질                                          │
│  • 테스트 커버리지                                           │
│  • 정적 분석 결과                                            │
│  • 보안 취약점                                               │
│                                                             │
│  📤 출력:                                                    │
│  • ✅ Approve: Agent 0에게 병합 승인 요청                    │
│  • ❌ Request Changes: 해당 Agent에게 수정 요청               │
│                                                             │
│  📍 실행 위치: sh/agent-N/ (각 워크트리에서 새 세션)          │
│                                                             │
│  💡 리뷰 명령어:                                             │
│  cd /mnt/c/.../sh/agent-N                                   │
│  claude   # 새 세션 실행                                      │
│  # 리뷰 프롬프트 입력                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### ④ Merge (Agent 0)

```
┌─────────────────────────────────────────────────────────────┐
│  단계 4: 병합 (Agent 0)                                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📋 입력:                                                    │
│  • 리뷰어의 Approve 판정                                     │
│  • 머지 대상 PR                                              │
│                                                             │
│  ✅ 수행 작업:                                               │
│  • PR 최종 확인                                              │
│  • develop 브랜치로 머지 (Squash Merge 권장)                 │
│  • 머지 충돌 해결 (필요 시)                                  │
│  • 브랜치 정리                                               │
│                                                             │
│  📍 실행 위치: sh/agent-0/ 또는 GitHub Web                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### ⑤ Sync

```
┌─────────────────────────────────────────────────────────────┐
│  단계 5: 동기화                                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📋 입력:                                                    │
│  • develop 브랜치 최신 상태                                  │
│                                                             │
│  🔄 수행 작업:                                               │
│  • sh/main → web/ 동기화                                    │
│  • Team GitLab에 푸시                                        │
│                                                             │
│  📍 실행 위치: S14P11C104/ (루트)                           │
│                                                             │
│  💡 명령어:                                                  │
│  chmod +x sync.sh   # 최초 1회                              │
│  ./sync.sh                                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 상태 흐름도 (State Machine)

```
                    ┌─────────────┐
                    │   PENDING   │
                    │  (대기 중)   │
                    └──────┬──────┘
                           │ Agent 0 지시
                           ▼
                    ┌─────────────┐
                    │ IN_PROGRESS │◄─────┐
                    │  (구현 중)   │      │
                    └──────┬──────┘      │
                           │ PR 생성     │
                           ▼             │
                    ┌─────────────┐      │
               ┌───►│  IN_REVIEW  │      │ Reject
               │    │  (검수 중)   │      │
               │    └──────┬──────┘      │
               │           │             │
               │           ├─────────────┘
               │           │ Approve
               │           ▼
               │    ┌─────────────┐
               │    │   MERGED    │
               │    │  (병합됨)   │
               │    └──────┬──────┘
               │           │ sync.sh
               │           ▼
               │    ┌─────────────┐
               │    │   SYNCED    │
               │    │  (동기화됨) │
               └────┴─────────────┘
                    다음 작업 지시
```

---

## 5. 파일 소유권 규칙

### 5.1 Agent 0 전용 문서 (소유권)

```
⚠️ Agent 0만 수정 가능한 파일:

/.agent/
  ├── PRD.md                 → Agent 0 전담
  ├── PLAN.md                → Agent 0 전담
  ├── PARALLEL-WORK.md       → Agent 0 전담
  ├── ADR.md                 → Agent 0 전담
  ├── HANDOFF.md             → Agent 0 전담
  └── workflows/             → Agent 0 전담
```

### 5.2 Agent 5 권한 (Read-Only + 리뷰)

```
📖 Agent 5의 권한:

모든 코드 파일:
  • Read-Only 접근 ✅
  • 리뷰 코멘트 작성 ✅
  • 직접 수정 ❌ (지양)

예외적 수정 허용:
  • .agent/SCRATCHPAD.md의 리뷰 섹션
  • 리뷰 과정 중 발견된 critical bug fix (Agent 0 승인 필요)
```

### 5.3 실무 Agent (1~4) 소유권

| Agent | Backend 소유 | Frontend 소유 |
|-------|-------------|---------------|
| **1** | `config/Security*`, `api/auth/*` | `features/auth/*`, `pages/Login/*` |
| **2** | `api/elder/*`, `api/emergency/*` | `features/elder/*`, `pages/Elder*/*` |
| **3** | `api/robot/*`, `scheduler/*` | `features/robot/*`, `pages/Robot/*` |
| **4** | `config/WebSocket*`, `api/common/*` | `shared/*`, `mocks/*` |

### 5.4 공유 파일 수정 규칙

```
⚠️ 다음 파일은 지정된 Agent만 수정:

/backend/
  └── build.gradle            → Agent 1 전담
  └── application.yml         → Agent 1 전담

/frontend/
  └── package.json            → Agent 2 전담
  └── src/app/router.tsx      → Agent 2 전담 (라우트 추가 시 요청)

/.agent/
  └── 모든 문서               → Agent 0 전담
  └── SCRATCHPAD.md           → 각 Agent 별도 섹션 사용
```

---

## 6. Phase별 다이어그램

### Phase 0: 인프라 설정 (병렬)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     Phase 0: 인프라 설정 (6인 체제)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────┐                              ┌───────────────────┐  │
│  │     Agent 0       │ ─ 지시 ─►  Agent 1~4  ◄─ 검수 ─│     Agent 5       │  │
│  │    COMMANDER      │                              │     AUDITOR       │  │
│  ├───────────────────┤                              ├───────────────────┤  │
│  │ 📁 sh/agent-0/    │                              │ 📁 sh/agent-5/    │  │
│  │ 🌿 mgmt/architect │                              │ 🌿 mgmt/reviewer  │  │
│  └───────────────────┘                              └───────────────────┘  │
│                                                                             │
│  ┌───────────────────┐  ┌───────────────────┐                              │
│  │     Agent 1       │  │     Agent 2       │                              │
│  │     BE-INFRA      │  │     FE-INFRA      │                              │
│  ├───────────────────┤  ├───────────────────┤                              │
│  │ 📁 sh/agent-1/    │  │ 📁 sh/agent-2/    │                              │
│  │ 🌿 phase0-be-infra│  │ 🌿 phase0-fe-infra│                              │
│  ├───────────────────┤  ├───────────────────┤                              │
│  │ Spring Boot       │  │ Vite+React        │                              │
│  │ PostgreSQL        │  │ Tailwind          │                              │
│  │ Security          │  │ Router            │                              │
│  │ REST Docs         │  │ TanStack Query    │                              │
│  │ Docker            │  │ Zustand           │                              │
│  └───────────────────┘  └───────────────────┘                              │
│                                                                             │
│  ┌───────────────────┐  ┌───────────────────┐                              │
│  │     Agent 3       │  │     Agent 4       │                              │
│  │     DB-SCHEMA     │  │     CONTRACTS     │                              │
│  ├───────────────────┤  ├───────────────────┤                              │
│  │ 📁 sh/agent-3/    │  │ 📁 sh/agent-4/    │                              │
│  │ 🌿 phase0-db-schema│ │ 🌿 phase0-contracts│                             │
│  ├───────────────────┤  ├───────────────────┤                              │
│  │ Flyway 설정        │  │ API 응답 형식      │                              │
│  │ ENUM 생성          │  │ Error 규격         │                              │
│  │ 7개 Entity        │  │ MSW 핸들러         │                              │
│  │ Repository        │  │ Axios 설정         │                              │
│  └───────────────────┘  └───────────────────┘                              │
│                                                                             │
│  의존성: 없음 (모두 독립 작업 가능)                                            │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Phase 1: 핵심 기능 (도메인별)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     Phase 1: 핵심 기능 (6인 체제)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────┐                              ┌───────────────────┐  │
│  │     Agent 0       │ ─ 지시 ─►  Agent 1~4  ◄─ 검수 ─│     Agent 5       │  │
│  │    COMMANDER      │                              │     AUDITOR       │  │
│  └───────────────────┘                              └───────────────────┘  │
│                                                                             │
│  ┌───────────────────┐  ┌───────────────────┐                              │
│  │     Agent 1       │  │     Agent 2       │                              │
│  │      AUTH         │  │  ELDER+EMERGENCY  │                              │
│  ├───────────────────┤  ├───────────────────┤                              │
│  │ 📁 sh/agent-1/    │  │ 📁 sh/agent-2/    │                              │
│  │ 🌿 phase1-auth    │  │ 🌿 phase1-elder   │                              │
│  ├───────────────────┤  ├───────────────────┤                              │
│  │ ⭐ CRITICAL       │  │ Auth 의존 (Mock)  │                              │
│  │ JWT 인증          │  │ 노인 CRUD         │                              │
│  │ 로그인/회원가입    │  │ 긴급 상황         │                              │
│  └───────────────────┘  └───────────────────┘                              │
│                                                                             │
│  ┌───────────────────┐  ┌───────────────────┐                              │
│  │     Agent 3       │  │     Agent 4       │                              │
│  │      ROBOT        │  │    WEBSOCKET      │                              │
│  ├───────────────────┤  ├───────────────────┤                              │
│  │ 📁 sh/agent-3/    │  │ 📁 sh/agent-4/    │                              │
│  │ 🌿 phase1-robot   │  │ 🌿 phase1-websocket│                             │
│  ├───────────────────┤  ├───────────────────┤                              │
│  │ Auth 의존 (Mock)  │  │ BE-INFRA 의존     │                              │
│  │ 로봇 제어         │  │ STOMP 설정        │                              │
│  │ 오프라인 판정     │  │ 실시간 토픽       │                              │
│  └───────────────────┘  └───────────────────┘                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 7. 머지 전략

### 머지 순서 (Phase 0)

```
1. Agent 3 (DB-SCHEMA) → develop
   └── Agent 5 검수 → Agent 0 승인
   └── Entity/Repository가 다른 모듈의 기반

2. Agent 1 (BE-INFRA) → develop
   └── Agent 5 검수 → Agent 0 승인
   └── DB 스키마 위에 설정 얹음

3. Agent 4 (CONTRACTS) → develop
   └── Agent 5 검수 → Agent 0 승인
   └── 공통 응답 형식 추가

4. Agent 2 (FE-INFRA) → develop
   └── Agent 5 검수 → Agent 0 승인
   └── 충돌 없음 (별도 디렉토리)
```

### 머지 순서 (Phase 1)

```
1. Agent 1 (AUTH) → develop ⭐ 최우선
   └── Agent 5 검수 → Agent 0 승인
   └── 다른 도메인이 인증 의존

2. Agent 4 (WEBSOCKET) → develop
   └── Agent 5 검수 → Agent 0 승인
   └── 실시간 기능 기반

3. Agent 2 (ELDER) + Agent 3 (ROBOT) → develop
   └── Agent 5 검수 → Agent 0 승인
   └── 순서 무관 (도메인 독립)
```

---

## 8. 충돌 방지 가이드

### 8.1 Git Worktree 제약 사항

> **경고**: 동일한 브랜치를 여러 워크트리에서 동시에 체크아웃할 수 없습니다!

```
❌ 금지되는 상황:

sh/agent-1/ → feature/phase1-auth (체크아웃 중)
sh/agent-2/ → feature/phase1-auth (체크아웃 시도) → ERROR!

✅ 올바른 상황:

sh/agent-0/ → management/architect
sh/agent-1/ → feature/phase1-auth
sh/agent-2/ → feature/phase1-elder
sh/agent-3/ → feature/phase1-robot
sh/agent-4/ → feature/phase1-websocket
sh/agent-5/ → management/reviewer
```

### 8.2 충돌 발생 시 대응

```bash
# 1. 충돌 발생한 워크트리에서
cd sh/agent-N
git status  # 충돌 파일 확인

# 2. 충돌 해결
# - 파일 직접 수정
# - 또는 담당 Agent에게 요청

# 3. 해결 후 커밋
git add <resolved-files>
git commit -m "fix: merge conflict 해결 [Agent N]"
```

---

## 9. 체크포인트 (Sync Points)

### Phase 0 완료 기준

```markdown
## 모든 Agent 작업 완료 후 확인

- [ ] Agent 0: 모든 작업 지시 완료
- [ ] Agent 1~4: 각 Worktree에서 정상 커밋/푸시 완료
- [ ] Agent 5: 모든 PR 검수 완료 및 Approve
- [ ] Agent 0: develop 병합 완료
- [ ] `docker-compose up` → PostgreSQL + App 정상 실행
- [ ] Flyway 마이그레이션 성공 (V1, V2)
- [ ] `npm run dev` → Frontend 정상 실행
- [ ] MSW Mock API 응답 확인
- [ ] 모든 Entity의 Repository 테스트 통과
- [ ] sync.sh 실행하여 Team Repo 동기화
```

### Phase 1 완료 기준

```markdown
## 통합 테스트

- [ ] Agent 5: 전체 코드 리뷰 완료
- [ ] 회원가입 → 로그인 → 노인 등록 → 로봇 명령 E2E
- [ ] 긴급 상황 발생 → WebSocket 알림 → 해제 E2E
- [ ] WORKER/FAMILY 역할별 라우팅 정상
- [ ] 오프라인 판정 스케줄러 동작 확인
- [ ] sync.sh 실행하여 Team Repo 동기화
```

---

## 10. 핵심 원칙

### Kent Beck의 병렬 작업 원칙

| 원칙 | 설명 | 적용 |
|------|------|------|
| **Independence** | 서로 의존하지 않는 작업 분리 | 도메인 경계 + Worktree 물리 분리 |
| **Tiny Steps** | 작은 단위로 쪼개기 | 1개 API = 1 커밋 |
| **Always Shippable** | 언제든 머지 가능 | 각 에이전트 결과물 독립 동작 |
| **Contract First** | 인터페이스 먼저 합의 | Phase 0에서 공통 규격 정의 |

### 병렬화 가능 조건

```
✅ 병렬 가능: 서로 다른 Worktree에서 작업
✅ 병렬 가능: 같은 도메인의 BE/FE (인터페이스 합의 후)
❌ 순차 필요: 공통 Entity → 이를 사용하는 Service
❌ 순차 필요: 인증 → 인증 필요한 API
❌ 금지: 같은 브랜치를 여러 Worktree에서 체크아웃
```

---

## 11. 빠른 참조 카드

### Agent별 작업 시작 명령어

```bash
# Agent 0 (지휘자)
cd /mnt/c/Users/SSAFY/Desktop/S14P11C104/sh/agent-0
git status
# 작업 지시서 작성...

# Agent 1
cd /mnt/c/Users/SSAFY/Desktop/S14P11C104/sh/agent-1
git status
# 구현 작업 시작...

# Agent 2
cd /mnt/c/Users/SSAFY/Desktop/S14P11C104/sh/agent-2
git status
# 구현 작업 시작...

# Agent 3
cd /mnt/c/Users/SSAFY/Desktop/S14P11C104/sh/agent-3
git status
# 구현 작업 시작...

# Agent 4
cd /mnt/c/Users/SSAFY/Desktop/S14P11C104/sh/agent-4
git status
# 구현 작업 시작...

# Agent 5 (검수자)
cd /mnt/c/Users/SSAFY/Desktop/S14P11C104/sh/agent-5
git fetch origin
# 리뷰 대상 브랜치 가져오기...
```

### 커밋 메시지 규칙

```
<type>(<scope>): <subject> [Agent N]

예시:
docs(plan): Phase 0 작업 지시서 작성 [Agent 0]
feat(auth): JWT 인증 필터 구현 [Agent 1]
feat(elder): 노인 CRUD API 구현 [Agent 2]
chore(db): V2 마이그레이션 스크립트 추가 [Agent 3]
feat(ws): WebSocket 토픽 설정 [Agent 4]
review(auth): 인증 모듈 코드 리뷰 완료 [Agent 5]
```

---

## 12. sync.sh 스크립트 (예시)

```bash
#!/bin/bash
# S14P11C104/sync.sh

echo "=== Syncing sh/main → web/ ==="

# 1. sh/main 최신화
cd sh/main
git checkout develop
git pull origin develop

# 2. web/ 디렉토리로 복사
cd ../..
rsync -av --exclude='.git' sh/main/ web/

# 3. web/ 커밋 (GitLab)
cd web
git add .
git commit -m "sync: $(date +%Y-%m-%d) 변경사항 동기화"
git push origin develop

echo "=== Sync Complete ==="
```

---

## 13. FAQ

### Q1: Agent 0은 어떤 기준으로 작업을 분배하나요?

**A**: PRD.md의 요구사항과 PLAN.md의 기술 설계를 분석하여, 각 Agent의 전문 영역과 현재 Phase에 맞게 작업을 분배합니다. 의존성이 있는 경우 순서를 고려합니다.

### Q2: Agent 5가 Reject하면 어떻게 되나요?

**A**: 해당 Agent(1~4)에게 수정 요청이 전달되고, 수정 완료 후 다시 Agent 5가 검수합니다. Approve될 때까지 이 루프가 반복됩니다.

### Q3: Agent 5가 직접 코드를 수정해도 되나요?

**A**: 원칙적으로 지양합니다. 단, critical bug의 경우 Agent 0의 승인 하에 최소한의 수정이 가능합니다.

### Q4: 다른 Agent의 코드가 필요한데 아직 머지되지 않았어요

**A**: Mock을 사용하거나, 해당 Agent의 브랜치를 로컬에서 fetch하여 참조하세요.

```bash
# 다른 Agent 브랜치 가져오기 (읽기 전용)
git fetch origin feature/phase0-db-schema
git log origin/feature/phase0-db-schema --oneline
```

### Q5: Worktree를 잘못 생성했어요

**A**: 제거 후 다시 생성하세요.

```bash
git worktree remove ../agent-N --force
git worktree add ../agent-N -b <correct-branch>
```

### Q6: 같은 브랜치를 체크아웃하려는데 에러가 나요

**A**: Git Worktree는 같은 브랜치를 여러 곳에서 체크아웃하는 것을 금지합니다. 해당 브랜치를 사용 중인 Worktree를 먼저 정리하세요.

---

## 14. 코드 리뷰 워크플로우 (Boris Cherny 방식)

> **원칙**: "Claude에게 검증 방법을 주면 결과 품질이 2-3배 향상된다"
> **참고**: [Boris Cherny's Claude Code Workflow](https://www.infoq.com/news/2026/01/claude-code-creator-workflow/)

### 14.1 리뷰 파이프라인

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         코드 리뷰 파이프라인                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  [작업 Phase]                                                               │
│  Agent 1~4: 각자 워크트리에서 작업 완료                                       │
│         ↓                                                                   │
│  각 Agent가 "코드 리뷰 요청 프롬프트" 작성                                    │
│         ↓                                                                   │
│  [검증 Phase]                                                               │
│  새 Claude 4개 (리뷰용): 각 프롬프트로 병렬 리뷰                              │
│                                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│  │ Agent 1     │    │ Agent 2     │    │ Agent 3     │    │ Agent 4     │  │
│  │ 작업 완료   │    │ 작업 완료   │    │ 작업 완료   │    │ 작업 완료   │  │
│  │     ↓       │    │     ↓       │    │     ↓       │    │     ↓       │  │
│  │ 리뷰 프롬프트│    │ 리뷰 프롬프트│    │ 리뷰 프롬프트│    │ 리뷰 프롬프트│  │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘    └──────┬──────┘  │
│         ↓                  ↓                  ↓                  ↓         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│  │ Reviewer A  │    │ Reviewer B  │    │ Reviewer C  │    │ Reviewer D  │  │
│  │ (새 Claude) │    │ (새 Claude) │    │ (새 Claude) │    │ (새 Claude) │  │
│  └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 14.2 코드 리뷰 요청 프롬프트 템플릿

각 Agent는 작업 완료 시 아래 템플릿으로 리뷰 요청 프롬프트를 작성합니다.

```markdown
## 코드 리뷰 요청 [Agent N]

### 작업 정보
- 브랜치: feature/phase{N}-{domain}
- 작업 범위: PLAN.md 섹션 X.X
- PR 링크: (있다면)

### 변경 파일
| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `backend/src/.../SecurityConfig.java` | 신규 | Spring Security 설정 |
| `frontend/src/app/router.tsx` | 수정 | 라우트 3개 추가 |

### 주요 변경 사항
1. [변경 사항 1]
2. [변경 사항 2]
3. [변경 사항 3]

### 검증 포인트 (리뷰어가 확인해야 할 것)
- [ ] 보안 취약점 없는지 (SQL Injection, XSS, CSRF 등)
- [ ] 기존 코드와 스타일 일관성 (CLAUDE.md 준수)
- [ ] RULES.md 위반 사항 없는지
- [ ] 테스트 커버리지 충분한지
- [ ] 에러 핸들링 적절한지

### 테스트 명령어
```bash
# Backend
cd backend && ./gradlew test

# Frontend
cd frontend && npm run test
```

### 우려 사항 / 특별 검토 요청
- [불확실한 부분이나 리뷰어에게 특별히 봐달라는 부분]
- 예: "SecurityConfig의 permitAll 경로가 너무 넓은지 확인 필요"
```

### 14.3 리뷰어용 Claude 실행 가이드

```bash
# 1. 새 터미널(WSL) 4개 열기

# 2. 각 터미널에서 리뷰 대상 워크트리로 이동
cd /mnt/c/Users/SSAFY/Desktop/S14P11C104/sh/agent-N

# 3. 리뷰 대상 브랜치 확인
git log --oneline -10
git diff develop..HEAD

# 4. 새 Claude Code 실행 후 리뷰 프롬프트 입력
claude

# 5. 리뷰 프롬프트 붙여넣기 + 추가 지시
"위 프롬프트의 검증 포인트를 기준으로 코드 리뷰해줘.
문제 발견 시 severity(critical/major/minor)와 함께 보고해줘."
```

### 14.4 리뷰 결과 보고 형식

리뷰어 Claude는 아래 형식으로 결과를 보고합니다.

```markdown
## 코드 리뷰 결과 [Agent N → Reviewer X]

### 요약
- 전체 평가: ✅ Approve / ⚠️ Request Changes / ❌ Reject
- Critical 이슈: 0건
- Major 이슈: 1건
- Minor 이슈: 3건

### 발견된 이슈

#### [Major] SecurityConfig.java:45 - 과도한 permitAll
```java
// 현재 코드
.requestMatchers("/api/**").permitAll()

// 제안
.requestMatchers("/api/auth/**").permitAll()
.requestMatchers("/api/**").authenticated()
```
**이유**: 모든 API가 인증 없이 접근 가능하여 보안 위험

#### [Minor] router.tsx:23 - 미사용 import
```typescript
import { useEffect } from 'react'; // 사용되지 않음
```

### 테스트 실행 결과
```
./gradlew test → PASSED (45/45)
npm run test → PASSED (12/12)
```

### 최종 의견
[총평 및 머지 가능 여부]
```

### 14.5 리뷰 완료 후 처리

```
1. Approve인 경우:
   → Agent 0이 develop에 머지 진행

2. Request Changes인 경우:
   → 해당 Agent가 수정 후 다시 리뷰 요청
   → 수정 범위가 작으면 같은 리뷰어가 재검토

3. Reject인 경우:
   → Critical 이슈 해결 후 전체 재리뷰
```

---

> **다음 단계**: 이 문서 확인 후 → 2.2~2.3 Worktree 생성 명령어 실행 → Agent 0이 작업 지시 시작
