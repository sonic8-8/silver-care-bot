# Phase 2 수정 지시 [Agent 4]

## 대상 브랜치
`feature/phase2-notification-realtime`

## 기준 리뷰
- `agent-4/.agent/reviews/REVIEW-RESULT-P2-AGENT4.md`
- 최신 판정: `✅ Approve` (Critical 0 / Major 0 / Minor 0)

## 지시 사항
1. 추가 코드 수정 없음 (Fix 종료).
2. 승인된 작업본의 원격 동기화 상태만 유지한다.
- `git push origin feature/phase2-notification-realtime` 상태 확인
- Agent 3 머지 후 develop 동기화(리베이스 또는 동등 절차) 수행
3. C-01 관련 최종 검증 지원:
- Agent 3의 `V5` 포함 통합 브랜치에서 `V6` 결합 검증이 필요하면 결과 로그를 공유한다.

## 병합 게이트
- Agent 4는 Agent 3, Agent 1 머지 완료 후 병합한다.
